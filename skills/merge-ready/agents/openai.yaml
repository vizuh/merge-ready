interface:
  display_name: "Merge Ready"
  short_description: "Human-gated multi-model PR readiness; never merge"
  default_prompt: "Use $merge-ready to route this branch across task-appropriate read-only reviewers, return every finding for approval, prepare its PR without merging it, and prune only after merge is confirmed."
